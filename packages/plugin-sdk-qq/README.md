# @qq-bot/plugin-sdk-qq

Optional QQ Official extensions for `@qq-bot/plugin-sdk`.

The base SDK remains platform-neutral. Import this package only when a plugin
intentionally uses QQ-specific capabilities such as wakeup delivery, keyboard
templates, or precomputed multipart-upload checksums.
