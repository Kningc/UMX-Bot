# @qq-bot/core

The lifecycle, command routing, middleware, scoped storage and plugin runtime
used by qq-bot applications and `@qq-bot/plugin-testkit`.

Plugin packages should depend on `@qq-bot/plugin-sdk`, not this package.
